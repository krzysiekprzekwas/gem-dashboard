# =============================================================================
# GEM DASHBOARD ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local for local development
# DO NOT commit .env.local to version control
#
# Usage: cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend Configuration (Next.js)
# -----------------------------------------------------------------------------

# API Base URL
# - In development: Use http://localhost:8000/api if running backend separately
# - In production (Vercel): Use relative path /api (handled by rewrites)
# - Default: /api (uses Next.js API rewrites)
NEXT_PUBLIC_API_URL=/api

# -----------------------------------------------------------------------------
# Backend Configuration (FastAPI)
# -----------------------------------------------------------------------------

# Cache Duration (minutes)
# - How long to cache ticker data from Yahoo Finance before refetching
# - Reduces API calls and improves response time
# - Default: 15 minutes
CACHE_DURATION_MINUTES=15

# Database Configuration
# - SQLite is used by default (file: backend/database.db)
# - For production, consider PostgreSQL on Vercel
# - Example PostgreSQL: postgresql://user:password@host:port/database
# DATABASE_URL=sqlite:///./database.db

# -----------------------------------------------------------------------------
# Production Only (Vercel)
# -----------------------------------------------------------------------------

# Cron Job Secret
# - Secret token for authenticating scheduled cron jobs
# - Generate a strong random token: openssl rand -hex 32
# - Set this in Vercel environment variables
# CRON_SECRET=your-secret-token-here

# Database URL (Vercel Postgres)
# - Automatically set by Vercel when using Vercel Postgres
# - Example: postgres://user:password@host.vercel-storage.com:5432/database
# DATABASE_URL=

# -----------------------------------------------------------------------------
# Analytics & Monitoring (Vercel)
# -----------------------------------------------------------------------------

# Vercel Analytics & Speed Insights
# - Automatically enabled on Vercel production deployments
# - Disabled by default in local development
# - Tracks page views, Core Web Vitals, and performance metrics
# - Privacy-friendly (no cookies, GDPR compliant)
# - View data: Vercel Dashboard → Analytics & Speed Insights tabs

# -----------------------------------------------------------------------------
# Development Tips
# -----------------------------------------------------------------------------

# 1. Local Development:
#    - Frontend: npm run dev (runs on http://localhost:3000)
#    - Backend: cd backend && python main.py (runs on http://localhost:8000)
#    - Set NEXT_PUBLIC_API_URL=http://localhost:8000/api
#
# 2. Testing with Next.js rewrites:
#    - Keep NEXT_PUBLIC_API_URL=/api
#    - Backend must be running on port 8000
#    - Next.js will proxy /api/* to http://localhost:8000/api/*
#
# 3. Cache Tuning:
#    - Lower CACHE_DURATION_MINUTES for more frequent updates
#    - Higher values reduce Yahoo Finance API load
#    - Recommended: 15-60 minutes for production
